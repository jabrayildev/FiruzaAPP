import SwiftUI
import UIKit

enum AppColors {
    
    static let background = Color(hex: "#fcfffe")
    static let background2 = Color(hex: "#ecfff8")
    
    static let textPrimary = Color(hex: "#001a0f")
    static let primary = Color(hex: "#05b571")
    
    static let circle = Color(hex: "#f0f9f6")
    static let white = Color(hex: "#ffffff")
    
    static let textPrimaryDefault = Color(hex: "#a1b2ab")
    static let primaryDefault = Color(hex: "#8fb2a5")
    
    static let card1 = Color(hex: "dbf0eb")
    static let card2 = Color(hex: "fff7ee")
}
