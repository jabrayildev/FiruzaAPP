import SwiftUI

struct HeaderRightActionsView: View {
    var body: some View {
        Button(action: {
            
        }) {
            Image ("notificationDefault")
            }
        
        Button(action: {
            
        }) {
            Image ("setting")
        }
        }
    }
